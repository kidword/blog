迭代器：是访问数据集合内元素的一种方式，一般用来遍历数据，但他不能想列表一样使用下标来获取元素数据，就是说迭代器不能被返回。

迭代器与可迭代对象：
    迭代器对象：可以用next获取下一个值，但是每个值只能获取一次，单纯的迭代器没有实现iter魔法函数，所以不能for循环；
    可迭代对象：只要可以用for循环的都是可迭代对象；
    可以将迭代器对象转化为可迭代对象；

生成器：函数中只要有yield，这个函数就变成生成器，，每次运行到yield的时候，函数会暂停，保存当前的运行状态，返回当前数值，并在下一次执行next方法的时候
，又从当前位置继续往下走。